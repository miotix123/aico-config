# AI Chat Organizer — selector config

Live DOM-selector configuration for the AI Chat Organizer browser extension.
When ChatGPT / Claude / Gemini change their interface, this file is updated so
installed extensions adapt automatically (checked ~every 6 hours).

`selectors.json` — current selector set. Bump `version` on every change.
